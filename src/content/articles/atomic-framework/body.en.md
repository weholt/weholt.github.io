## Draft

Atomic is a framework idea for structured, git-driven, agent-assisted development.

The core idea is simple: keep work small, explicit, reviewable and traceable. Plans, tasks, implementation loops, validation and retrospectives should live close to the repository and be understandable by both humans and agents.

```text
plan -> issues -> implementation loop -> review -> validation -> retrospective
```
